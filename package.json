{
  "name": "lambda-kronos-apiservice-root",
  "version": "1.0.0",
  "scripts": {
    "build": "sam build",
    "deploy": "sam deploy",
    "local:api": "sam build && sam local start-api --port 3001"
  }
}
