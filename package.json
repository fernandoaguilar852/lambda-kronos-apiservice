{
  "name": "lambda-kronos-auth-root",
  "version": "1.0.0",
  "scripts": {
    "build": "sam build",
    "deploy": "sam deploy",
    "local:api": "sam build && sam local start-api --port 3001"
  }
}
